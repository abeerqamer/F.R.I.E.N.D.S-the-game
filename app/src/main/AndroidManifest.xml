<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.friends">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.FRIENDS.android.friendsthegame.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.FRIENDS.android.friendsthegame.questionDisplay" />
        <activity android:name="com.FRIENDS.android.friendsthegame.ResultActivity"

            android:label="RESULT"

            >



        </activity>
    </application>

</manifest>